# decifra research UI (Phase 5 MVP)

Dark-mode React app that renders the three standardized schemas from sample JSON.

```bash
cd frontend
npm install
npm run dev
```

Samples live in `public/sample/`. Streamlit remains the interim research UI until cutover.
